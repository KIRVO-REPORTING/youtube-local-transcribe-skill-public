interface:
  display_name: "Video Report Builder"
  short_description: "Build timestamp-linked video reports locally"
  default_prompt: "Use $youtube-local-transcribe for this video link: prefer captions, fall back to local Whisper, write timestamped key points, finalize report.html, and update the dashboard."
policy:
  allow_implicit_invocation: true
