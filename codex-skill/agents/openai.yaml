interface:
  display_name: "Video to Notes"
  short_description: "Turn bare video URLs into timestamped reports"
  default_prompt: "Use $video-to-notes to process this bare video URL immediately and publish the completed timestamped report."
policy:
  allow_implicit_invocation: true
